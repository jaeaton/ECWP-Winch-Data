<UserControl x:Class="Views.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Views"
             xmlns:lc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF" 
             xmlns:vm="clr-namespace:ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.DataContext>
            <vm:ChartDataViewModel/>
        </Grid.DataContext>
        <lc:CartesianChart x:Name="Chart"  
                           Series="{ Binding Path=Series }" 
                           XAxes="{Binding XAxes}"  
                           YAxes="{Binding YAxes}"
                           EasingFunction="{x:Null}" />
        <!--<lc:CartesianChart x:Name="Chart2"  
                           Series="{ Binding Path=SeriesZero }" 
                           XAxes="{Binding XAxes}"  
                           YAxes="{Binding YAxes}"
                           EasingFunction="{x:Null}" />-->
    </Grid>
</UserControl>
